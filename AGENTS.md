# MythSurvivor Agent 规则

本文档用于约束所有后续参与 MythSurvivor 项目的 Codex Agent。

## 项目定位

- 项目名称：MythSurvivor。
- 游戏引擎：Godot 4.x。
- 游戏维度：2D。
- 主要语言：GDScript。
- 游戏类型：中国神话题材、俯视角、自动战斗、Roguelite 生存游戏。
- 当前阶段：MVP v0.1 已完成，进入 v0.2 功能扩展与体验打磨阶段。

## 当前系统状态

项目已经完成 MVP v0.1，并已推送到 GitHub。

当前已完成系统：

- Player System。
- Enemy System。
- Weapon System。
- XP / Level System。
- UI System。
- QA Polish。
- Release v0.1.0-mvp。

后续开发应默认基于现有 MVP 系统增量扩展，不要把项目当作纯文档阶段或空项目处理。



## 技术基线

- 使用 Godot 4.x 项目约定。
- 游戏代码默认使用 GDScript，除非用户明确批准其他语言。
- 游戏形态为 2D 俯视角。
- 优先使用场景组合、节点、信号、资源和数据驱动配置，避免大型单体脚本。
- 在可行范围内保持系统确定性，尤其是战斗计算、刷怪规则、升级选择和成长状态。
- 使用特定 Godot 4.x API 前，应确认其兼容性。
- 新功能应优先延续现有 MVP 结构，只有在需求确实推动架构变化时才引入新抽象。

## 预期目录结构

项目后续扩展时应采用以下结构：

```text
res://
  assets/
    audio/
    fonts/
    sprites/
    vfx/
  docs/
  scenes/
    main/
    gameplay/
    actors/
    ui/
  scripts/
    core/
    gameplay/
    actors/
    combat/
    progression/
    ui/
    utils/
  resources/
    enemies/
    weapons/
    upgrades/
    waves/
    loot/
  tests/
```

目录结构应随 v0.2 功能实现逐步落地。创建新目录、脚本、场景或资源时，必须符合当前任务范围和模块职责。

## 默认 Subagents 工作流

后续开发默认使用主控 Agent / Orchestrator + 固定 Subagents 工作流。

用户只需要向主控 Agent 描述需求，例如“新增雷法技能”“做暂停菜单”“加受击反馈”或“优化刷怪节奏”。主控 Agent 必须自动判断任务类型，并在涉及功能实现时默认调用完整固定流程：

- `pm_planner`。
- `code_explorer`。
- `feature_worker`。
- `reviewer`。
- `qa_tester`。

默认职责顺序：

1. `pm_planner` 规划任务范围、分支名、允许修改文件、禁止修改文件和验收标准。
2. `code_explorer` 只读分析现有代码、场景、资源和接入点。
3. `feature_worker` 在授权范围内实现功能。
4. `reviewer` 在提交前只读审查 diff，检查越界修改、明显 bug 和系统回归风险。
5. `qa_tester` 在提交前输出 Godot 手动测试清单和回归测试项。
6. 主控 Agent 汇总 Subagents 结果、验证状态、风险点和下一步建议。

只有 `feature_worker` 默认允许写代码、场景、资源或配置。`pm_planner`、`code_explorer`、`reviewer` 和 `qa_tester` 默认只读，不得修改文件。

`reviewer` 和 `qa_tester` 必须在提交前参与功能开发流程。除非用户明确要求跳过，否则主控 Agent 不应在缺少 review 或 QA 测试清单的情况下提交功能改动。

主控 Agent 不允许自动 commit、push、merge，除非用户明确要求。即使功能已经实现并通过 review，提交和推送也必须等待用户明确授权。

### 主控 Agent / Orchestrator

- 接收用户需求并确认范围、限制和当前工作区状态。
- 根据任务调用合适的 Subagents。
- 控制 Subagent 数量，避免无意义并行、重复分析或越界实现。
- 汇总 Subagent 结果，给出可执行结论和后续建议。
- 不默认直接写代码；实现工作默认交给 `feature_worker`，除非用户明确要求主控直接处理小范围修改。
- 不自动 commit、push、merge，除非用户明确要求。
- 发现用户请求与本规则冲突时，先指出冲突，再继续执行可行部分。

### pm_planner

- 只负责需求拆解、开发顺序、风险、分支建议和验收标准。
- 只规划，不改代码、不改场景、不改资源。

### code_explorer

- 只读分析现有代码、场景、资源、文档和 Git 状态。
- 输出实现建议、依赖关系、风险点和候选修改文件。
- 不改代码、不改场景、不改资源、不执行破坏性命令。

### feature_worker

- 唯一默认允许实现功能的子 Agent。
- 只能修改用户或主控明确授权的文件范围。
- 不得越界修改无关系统，不得覆盖其他 Agent 或用户的未提交工作。
- 默认不 commit、不 push、不 merge。
- 完成后必须报告修改文件、验证命令、未验证项和风险。

### reviewer

- 只读审查当前 diff。
- 优先检查 bug、行为回归、越界修改、场景引用损坏、Godot/GDScript 解析风险和测试缺口。
- 不改代码、不自动修复、不提交。

### qa_tester

- 输出手动测试清单、回归范围、验收标准和必要的自动化测试建议。
- 默认不改代码。
- 如用户明确要求编写测试脚本，必须先确认允许修改的测试文件范围。

## Agent 职责边界

每个 Agent 必须将修改范围控制在自己的职责内，不要默认接管无关系统。

### 架构 Agent

- 负责高层技术方向、模块边界、场景层级、数据流和集成规则。
- 维护 `docs/ARCHITECTURE.md`、`AGENTS.md` 和跨系统协作规则。
- 除非被明确要求，不实现具体玩法功能。

### 玩法 Agent

- 负责玩家移动、敌人行为、战斗规则、刷怪、武器、拾取物、升级和单局循环。
- 必须遵守架构规则，并保持战斗数值原创。
- 在可行范围内保持玩法脚本数据驱动。

### UI/UX Agent

- 负责 HUD、菜单、暂停流程、升级选择、游戏结束界面、输入提示和视觉层级。
- 必须创建原创 UI 布局，不要克隆现有商业生存 Roguelite 游戏的界面。
- 与玩法 Agent 协调需要展示的数据契约。

### 内容 Agent

- 负责神话灵感命名、简短设定、敌人概念、武器概念、升级概念、关卡主题和物品描述。
- 必须使用原创方式表达中国神话灵感，不得复制商业作品中的解释和表现。
- 在数值定稿前记录内容意图。

### 美术/音频 Agent

- 负责原创视觉和音频资源规划、导入设置、命名规范和资源许可证记录。
- 不得添加受版权保护或未授权的商业资源。
- 如使用临时占位资源，必须清楚标记为占位内容。

### QA/测试 Agent

- 负责手动测试清单、可行的自动化测试计划、回归记录和缺陷复现步骤。
- 验证修改不会导致 Godot 导入错误、场景引用损坏或脚本解析错误。

### 构建/发布 Agent

- 负责导出预设、平台说明、版本管理、打包和发布清单。
- 除非被明确要求，不在构建工作中修改玩法系统。

## 协作规则

- 修改架构或实现前，先阅读现有文档。
- 不要在未理解的情况下覆盖其他 Agent 的工作。
- 保持提交和补丁聚焦。
- 优先做小而可审查的修改，避免大范围重写。
- 当决策影响代码结构、数据归属或 MVP 范围时，同步更新文档。
- 如果用户请求与本规则冲突，应先指出冲突，再继续编辑。
- 修改游戏功能时，应同步考虑手动测试清单和回归风险。
- 不要把临时调试代码、未使用资源或无关格式化混入功能补丁。

## 分支规则

- `main` 永远保持稳定。
- 每个功能、修复或实验使用独立分支。
- 工作区不干净时，不允许创建新功能分支；必须先让用户确认如何处理现有改动。
- 不允许 force push，除非用户明确确认。
- 不允许自动合并 `main`，除非用户明确要求并确认风险。
- 不自动 commit、push、merge；这些 Git 操作必须由用户明确授权。
- 切分支前必须检查当前分支和 `git status`。

## 当前阶段工作规则

当前 v0.2 阶段允许在用户明确任务范围内创建或修改游戏功能脚本、场景、资源和测试文件。

所有实现仍必须遵守：

- 只修改任务授权范围内的文件。
- 不修改无关系统。
- 不削弱原创性与版权边界。
- 不破坏 MVP v0.1 已完成的核心循环。
- 不默认修改 `project.godot`；只有入口场景、输入映射、导出配置等确实需要时，才可在用户授权范围内修改。
- 不自动添加外部资源；新增资源必须确认许可证兼容或为原创占位内容。
